import { Time2BlocksPipe } from './time2blocks.pipe';

describe('Time2blocksPipe', () => {
  it('create an instance', () => {
    const pipe = new Time2BlocksPipe();
    expect(pipe).toBeTruthy();
  });
});
